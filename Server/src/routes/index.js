const handleUsers = require('./handleUsers.js');
const handleFavorites = require('./handleFavorites.js');
const handleCharacters = require('./handleCharacters.js');

module.exports = {
    handleUsers,
    handleFavorites,
    handleCharacters
};